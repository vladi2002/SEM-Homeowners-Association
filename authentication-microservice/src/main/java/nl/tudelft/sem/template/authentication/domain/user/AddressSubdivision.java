package nl.tudelft.sem.template.authentication.domain.user;

/**
 * Enum that can be used when referring to a subdivision of an address (In case of an address change etc.)
 */
public enum AddressSubdivision {
    country,
    city,
    street,
    houseNumber,
    postalCode
}
